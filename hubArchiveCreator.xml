<tool id="hubArchiveCreator" name="Hub Archive Creator" version="1.6.0">
    <description>
        This Galaxy tool permits to prepare your files to be ready for
        Assembly Hub visualization.
    </description>

    <requirements>
        <requirement type="package" version="312">ucsc_tools</requirement>
    </requirements>

    <stdio>
        <regex match="^pass1"
               source="stderr"
               level="log"
               description="bedToBigBed" />
        <!-- TODO: Add the case pass1 and 0 chroms -->
        <!-- TODO: Add the case pass2 and 0 records or 0 fields -->
    </stdio>

    <command><![CDATA[
        mkdir -p $output.files_path &&
        python $__tool_directory__/hubArchiveCreator.py -g $GFF3_Track -f $Fasta_File -d $__tool_directory__ -e $output.files_path -o $output;
    ]]></command>

    <inputs>
        <param
          format="gff3"
          name="GFF3_Track"
          type="data"
          label="GFF3 track to display" />

        <param
          format="fasta"
          name="Fasta_File"
          type="data"
          label="Fasta track to process" />
    </inputs>

    <outputs>
        <data format="huba" name="output" />
    </outputs>

    <tests>
        <test>
            <param name="GFF3_Track" value="augustusDbia3.gff3"/>
            <param name="Fasta_File" value="dbia3.fa"/>
            <output name="output">
                <assert_contents>
                    <has_text text="myHub" />
                </assert_contents>
            </output>
        </test>
    </tests>

    <help>
        This Galaxy tool permits to prepare your files to be ready for
        Assembly Hub visualization.
    </help>
</tool>
